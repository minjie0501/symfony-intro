{% extends 'base.html.twig' %}

{% block title %}{{title}}{% endblock %}

{% block body %}
	<br>
	<h1 class="font-bold text-2xl">
		Hello {{name|title}}!
	</h1>

	{% if (page == "change-page") %}
		{{ form(form) }}
			{% else %}
		{{form(form)}}
	{% endif %}
{% endblock %}
